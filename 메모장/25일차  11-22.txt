25일차  11-22

오라클

자료 

java stream

filter : where
map  : col1, col2, col3 from emp;

별명(alias)를 붙일때 띄어쓰기가 있으면 ""을 써줘야한다.

연결 연산자
|| 를 사용해서 두개의(이름 직급) 컬럼을 하나의 컬럼(ex 교수)으로 합친다.

'조교수'  ==> ''''

distinct
-중복제거하여 조회하기
-성능이 안좋다.
-한번만 사용이 가능하다.

where 조건문
select 조회하고자 하는 컬럼명 -- 생략불가
from 테이블명  -- 생략불가 
where 조회하고자 하는 레코드의 조건 설정

between 주의사항 
-두개의 값중에 작은값이 먼저오고 큰값이 나중에 와야한다
-두 개의 값을 다 포함하여 출력한다.

and 
두 가지 조건을 모두 만족하는 결과를 검색할때  and 연산자를 사용하여 조건을적으면된다.

or 
두 가지 조건중 한가지만 만족하는 행을 검색하려면 or연산자를  사용하면된다.

order by 절을 사용하여 추력 결과 정렬!
- asc 는 오름차순   == 디폴트
- desc 는 내림차순

union 
두 집합을 더해서 결과를 출력. 중복 값 제거하고 정렬
객체와 타입이 다 같아야 합칠수있음

union all 
두 집합을 더해서 결과를 출력. 중복 값 제거 안하고 정렬 안함
객체와 타입이 다 같아야 합칠수있음 

intersect 
두 집합의 교집합 결과를 출력함. 정렬함

minus
두 집합의 차집합 결과를 출력함. 정렬함. 쿼리의 순서 중요함

intersect
교집합 찾기 중복된 집합 출력

initcap
첫 글자만 대문자로 출력하고 나머지 전부 소문자 출력

length 
입력된 문자열의 길이를(바이트 수를) 계산해주는 함수

substr
('문자열'또는 컬럼명,1,4) 1부터 4개문자